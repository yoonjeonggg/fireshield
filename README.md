# FireShield

**소방기관 사칭 사기 실시간 진위확인 시스템**

> 2026 소방안전 빅데이터 활용 아이디어 경진대회 출품작
> https://bigdata-119.kr/bbs/view?bbsctt_id=571

## 배경

최근 소방서·소방공무원을 사칭해 "법령 개정으로 소화기 교체가 의무화됐다", "정부 보조금이 전액 환급된다"는 명목으로 금전을 요구하는 사기가 전국적으로 발생하고 있습니다 (경북 6.3억 원, 제주 8,070만 원 피해 사례 등). 피해자 대부분은 민박·모텔·단란주점·노래연습장 같은 소규모 다중이용업소 사업주로, 사칭 여부를 확인할 수단이 없어 그대로 송금하는 경우가 많습니다.

**FireShield는 송금 직전, 그 자리에서 즉시 진위를 확인할 수 있는 웹서비스입니다.** 설치나 회원가입 없이 URL 접속만으로 확인할 수 있도록 만들어, 골든타임 안에 판단할 수 있도록 설계했습니다.

## 핵심 기능

### 1. 소방기관 사칭 진위확인 (`/`)
사용자가 입력한 발신 기관명, 담당자명, 요구 사유, 법령명, 요구받은 계좌번호를 아래 공공데이터와 실시간으로 대조하여 위험도(안전/의심/위험)를 판정합니다.

- **법제처 국가법령정보 공동활용 API** — 언급된 법령이 실제로 존재하는지, 최근 개정 이력이 있는지 실시간 조회
- **소방청 소방시설업 현황** (공공데이터포털, 전국 10,465건) — 언급된 업체가 실제 등록된 소방시설업체인지 대조
- **계좌번호 사기 신고 이력** — 같은 계좌번호가 최근 90일간 3회 이상 '위험/의심'으로 조회됐는지 자체 로그로 집계(경찰청 '사이버사기 피해신고 이력조회'와 동일 기준). 계좌번호는 원문 저장 없이 단방향 키 해시로만 대조하며, 결과 화면에서 경찰청·더치트 공식 조회 페이지로 딥링크 제공
- 계좌번호 요구 여부 등 행동 패턴을 종합해 0~100점 위험 점수 산출

### 2. 화재 위험지역 지도 (`/risk-map`)
소방청 전국 화재 현황 데이터를 기반으로 시/도별 위험도를 시각화하고, 특정 시/도를 선택하면 시/군/구 단위 상세 통계로 드릴다운할 수 있습니다.

## 데이터 처리 — 원본을 그대로 쓰지 않고 목적에 맞게 가공

이 프로젝트에서 신경 쓴 부분은 공공데이터를 그대로 붙이는 것이 아니라, **서비스 목적(사칭 판별)에 맞게 필터링하고 재구성**한 것입니다.

| 원본 데이터 | 문제 | 처리 방식 |
|---|---|---|
| 전국 화재 현황 (연간 38,409건) | "최근 3개월"이라는 서비스 맥락에 비해 1년치 데이터라 과다 | 데이터 내 최신 접수일 기준으로 최근 90일만 필터링 → 실사용 맥락에 맞는 8,652건으로 축소 |
| 전국 다중이용업소 현황 (24,547건) | 시/도 컬럼이 따로 없이 주소 텍스트에 섞여 있고, 폐업 이력이 필터링되지 않음 | 정규식으로 주소에서 시/도를 추출하고, `사용여부=Y`(실제 영업 중)인 건만 필터링 후 시/도별 집계 |
| 소방시설업 현황 (10,465건) | EUC-KR 인코딩, 결측치가 NaN(float)으로 남아 DB 저장 시 타입 오류 발생 | 인코딩 감지 후 안전한 문자열 정규화 함수로 전처리, 업종·상호·대표자 컬럼을 인덱싱해 부분일치 검색 최적화 |
| 화재현황 + 다중이용업소현황 (독립된 두 데이터셋) | 각각 별도로 제공되어 단순 합산만으로는 위험도를 판단하기 어려움 | 시/도 단위로 결합한 뒤, 화재 건수·사망자 수·다중이용업소 밀집도를 가중합산하여 0~100으로 정규화한 위험 점수로 재구성 |

## 기술 스택

### Backend — FastAPI
- **FastAPI + Python 3.12**, **SQLAlchemy 2.0 (async)** — 법제처 API 호출과 DB 조회가 동시에 발생하는 I/O 바운드 워크로드에서 비동기 처리로 응답 지연 최소화
- **Pydantic v2** — 요청 검증(계좌번호 형식, 공백 입력 방지 등)과 응답 스키마를 코드 레벨에서 강제
- **PostgreSQL 16 (Docker)** — 조회 로그 적재, 개인정보(계좌번호)는 AES-256-GCM으로 암호화하여 저장
- **httpx** — 법제처 XML API 비동기 호출 및 파싱
- **pandas** — 대용량 CSV(연 3만 건 이상)의 필터링·집계·정규화 배치 처리

### Frontend
- **Next.js 14 (App Router) + TypeScript**
- **Tailwind CSS** — 위험도 신호등 컬러 시스템(안전/의심/위험)을 적용한 디자인 시스템
- **Google Maps JavaScript API** — 시/도 단위 위험지역 시각화 및 드릴다운

## 아키텍처
[Next.js 프론트엔드]
│ REST API
▼
[FastAPI 백엔드]
│
├─ [PostgreSQL] 조회 로그(AES-256 암호화), 소방시설업 현황 캐시
│
└─ [외부 API / 데이터]
- 법제처 국가법령정보 공동활용 API (실시간)
- 소방청 소방시설업 현황 (공공데이터포털, 배치 적재)
- 소방청 전국 화재 현황 (필터링·집계 후 서빙)
- 전국 다중이용업소 현황 (필터링·집계 후 서빙)

## 실행 방법

### Backend
```bash
cd backend
conda create -n fireshield python=3.12
conda activate fireshield
pip install -r requirements.txt

docker compose up -d                      # PostgreSQL 실행
python scripts/load_fire_businesses.py    # 소방시설업 현황 데이터 적재

uvicorn app.main:app --reload
```
`.env` 파일에 `DATABASE_URL`, `LAW_API_KEY`, `ENCRYPTION_KEY` 등 환경변수 설정이 필요합니다.

### Frontend
```bash
cd frontend
npm install
npm run dev
```
`.env.local`에 `NEXT_PUBLIC_API_BASE_URL`, `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` 설정이 필요합니다.

## 향후 계획

- PostGIS 기반 공간쿼리로 다중이용업소·화재현황을 결합한 위험지역 세분화
- verify_logs 누적 데이터 기반 이상탐지 모델 도입
- 계좌 사기 이력 조회를 더치트 OpenAPI 등 외부 신고 데이터와 연동 (현재는 자체 로그 집계 + 공식 페이지 딥링크)
- AWS(ECS Fargate + RDS + Amplify) 배포
